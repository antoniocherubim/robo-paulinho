Task 1: Definir Estratégia de Provider
Objetivo: decidir como o sistema escolhe entre Anthropic/Claude e OpenAI.

Escopo:

definir variável principal de seleção, por exemplo LLM_PROVIDER
definir valores aceitos
definir comportamento default quando a variável não existir
definir ordem de fallback entre providers e mecanismos internos
Decisões a fechar:

LLM_PROVIDER=anthropic|openai|auto
no modo auto, tentar qual primeiro
se OpenAI falhar, cai para Anthropic ou não
se Claude SDK/CLI continuam existindo como fallback separado da Anthropic API
Entrega esperada:

documento/plano de decisão
contrato claro de seleção de provider
Critério de aceitação:

sem ambiguidade sobre qual provider será usado em cada cenário

---------------------------------------------------------------------------------

Task 2: Modelar Variáveis de Ambiente
Objetivo: preparar .env.example, config.py e README para suportar OpenAI.

Escopo:

adicionar variáveis como:
LLM_PROVIDER
OPENAI_API_KEY
OPENAI_MODEL
decidir se entram também:
OPENAI_MAX_TOKENS
OPENAI_TIMEOUT
manter compatibilidade com ANTHROPIC_API_KEY, ANTHROPIC_MODEL e paths do Claude
Entrega esperada:

atualização de .env.example
atualização de README.md
possivelmente novos defaults em config.py
Critério de aceitação:

configuração do novo provider documentada e consistente

---------------------------------------------------------------------------------------

Task 3: Extrair Contrato Comum de Resposta LLM
Objetivo: garantir que Anthropic e OpenAI retornem texto no mesmo formato interno.

Escopo:

definir como cada backend devolve a resposta final
garantir que chamar_llm(...) continue retornando str | None
definir tratamento de erro uniforme entre providers
Entrega esperada:

contrato interno documentado
plano de adaptação mínima no llm.py
Critério de aceitação:

pipeline não precisa saber qual provider foi usado

-----------------------------------------------------------------------------------------

Task 4: Implementar Cliente OpenAI
Objetivo: adicionar integração com a API da OpenAI no llm.py.

Escopo:

criar algo como chamar_openai_api(...)
usar OPENAI_API_KEY
usar OPENAI_MODEL
adaptar para o mesmo contrato textual do Anthropic
incluir tratamento de rate limit e retries equivalente ao já existente
Entrega esperada:

novo backend OpenAI em llm.py
sem quebrar Anthropic/Claude atuais
Critério de aceitação:

chamada OpenAI funcional e isolada no módulo LLM

----------------------------------------------------------------------------------------

Task 5: Integrar Seleção de Provider em chamar_llm
Objetivo: fazer chamar_llm(...) decidir qual backend usar.

Escopo:

ler LLM_PROVIDER
chamar OpenAI ou Anthropic conforme configuração
opcionalmente suportar auto
preservar logs compreensíveis
Entrega esperada:

chamar_llm multi-provider
comportamento atual preservado quando configurado para Anthropic/Claude
Critério de aceitação:

troca de provider sem alterar o pipeline

------------------------------------------------------------------------------------------

Task 6: Definir Fallbacks e Prioridades
Objetivo: formalizar o que acontece quando o provider escolhido falha.

Escopo:

decidir se:
openai tenta só OpenAI
anthropic tenta Anthropic API -> SDK -> CLI
auto tenta OpenAI -> Anthropic API -> SDK -> CLI
alinhar mensagens de log
Entrega esperada:

política de fallback fechada
implementação coerente com a Task 5
Critério de aceitação:

comportamento previsível em erro ou ausência de credenciais

----------------------------------------------------------------------------------------------

Task 7: Atualizar Testes
Objetivo: cobrir a nova arquitetura sem depender de rede.

Escopo:

testes de imports
testes de seleção de provider
testes de fallback com monkeypatch/mock simples via unittest.mock
testes de leitura de variáveis de ambiente
Entrega esperada:

novos testes em tests/
cobertura mínima da lógica de escolha de provider
Critério de aceitação:

mudança protegida por testes locais

---------------------------------------------------------------------------------------------

Task 8: Atualizar Documentação de Uso
Objetivo: documentar claramente como rodar com Anthropic ou OpenAI.

Escopo:

README com exemplos de .env
exemplos para:
Anthropic API
OpenAI API
Claude CLI
explicar o valor de LLM_PROVIDER
Entrega esperada:

README atualizado
.env.example claro
Critério de aceitação:

alguém novo consegue configurar sem ler o código

---------------------------------------------------------------------------------------------

Task 9: Validação Final da Feature
Objetivo: fechar a feature com checklist de compatibilidade.

Escopo:

confirmar que:
Anthropic continua funcionando
OpenAI entra como opção
pipeline não mudou
artefatos finais continuam iguais
rodar py_compile e testes
validar que a API pública principal continua a mesma
Entrega esperada:

checklist final de aceite
base pronta para uso multi-provider
Critério de aceitação:

feature entregue sem regressão estrutural